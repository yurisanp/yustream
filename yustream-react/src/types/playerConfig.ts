export interface PlayerConfig {
  /** ID do evento configurado no Vimeo */
  videoId: string
  updatedAt?: string
  updatedBy?: string
}

export interface PlayerConfigInput {
  /** ID do evento configurado no Vimeo */
  videoId: string
}

